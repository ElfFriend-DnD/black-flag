name: Checks

on:
  - pull_request

env:
  node_version: 18

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup
        uses: ./.github/actions/setup

      - name: Lint
        run: npm run lint
