name: Checks

on:
  - pull_request

env:
  node_version: 18

jobs:
  # Use a Common Setup workflow
  setup_action:
    steps:
      - uses: ./.github/actions/setup

  lint:
    needs: call_setup
    runs-on: ubuntu-latest
    steps:
      - name: Lint
        run: npm run lint
